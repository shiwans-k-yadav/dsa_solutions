class Solution {
public:
    bool rotateString(string s, string goal) {
        int n = s.length();
        int i=0;
        while(i<n){
            if(s==goal){
                return true;
            }
            char ch=s[0];
            for(int i=0;i<n-1;i++){
                s[i]=s[i+1];
            }
            s[n-1]=ch;
            i++;
        }
        return false;
    }
};
